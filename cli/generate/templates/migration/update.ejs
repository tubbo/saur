import Migration from "https://deno.land/x/saur/model/migration.js";

export default class <%= className %> extends Migration {
  up(db) {
    db.table("<%= tableName %>").create({
      <%= fields %>
    });
  }

  down(db) {
    db.table("<%= tableName %>").drop();
  }
}
